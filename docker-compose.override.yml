version: '3'
services:
    web:
        volumes:
            - /deliveryApi:/deliveryApi
        ports:
            - "80:80"
        environment:
            - FLASK_APP=main.py
            - FLASK_DEBUG=1
            - 'RUN=flask run --host=0.0.0.0 --port=80'
        command: python3 main.py